streamlit==1.40.0
pandas==2.2.2
numpy==1.26.4
requests==2.32.3
polygon-api-client==1.13.7
supabase==2.5.0
websockets==11.0.0
setuptools>=69.0.0
# NOT for building. It was added so `pkg_resources` would exist for an older
# polygon-api-client that imported it unguarded; 1.13.7 uses
# importlib.metadata instead, and setuptools >= 81 dropped pkg_resources
# entirely, so this pin no longer supplies it. Kept because nothing has
# proven Streamlit Cloud's image works without it.
#
# `pkg_resources.py` AT THE REPO ROOT is the thing that actually answers
# `import pkg_resources` today -- the repo root precedes site-packages on
# sys.path, so the shim SHADOWS the real module for the whole app. It greps
# as imported-by-nothing and is one cleanup sweep away from being deleted.
# See its docstring before touching either.

sentry-sdk==2.66.1

# torch and transformers deliberately REMOVED. FinBERT runs in the `inference`
# Railway service; utils.sentiment calls it over HTTP. They were ~2GB of the
# install (torch alone ~1.6GB plus 12 nvidia-* CUDA packages that are pure
# waste on a CPU box) and ~886MB resident once loaded, against a ~1GB tier --
# which is what OOM-killed a scan mid-run on 2026-08-01.
#
# With them gone there is no local fallback, which is the INTENDED end state:
# if inference is unreachable, analyze_sentiment_batch raises and the caller's
# try/finally refunds the credit. A failure the user is told about beats a
# silent fallback nobody notices -- the service ran 40x degraded for hours
# precisely because the fallback hid it.

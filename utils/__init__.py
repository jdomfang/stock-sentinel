"""Local application utilities.

This file makes the `utils` directory a proper Python package so imports like
`from utils.navigation import ...` resolve to the local code (and not any
third-party `utils` module).
"""
